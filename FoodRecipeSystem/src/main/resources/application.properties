##Spring DATASOURCE(DataSourceAutoConfiguration and DataSourceProperties)
spring.datasource.url="jdbc:mysql://localhost:3306/myUsers" ?useSSL=false
spring.datasource.username=Shilpa
spring.datasource.password=Jerry123@

##Hibernate Properties

#The SQL dialect makes Hibernate generate better SQL for the choosen database.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

#Hibernate DDL auto (create, create-drop,validate,update)
spring.jpa.hibernate.ddl.auto=update
